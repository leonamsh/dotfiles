
{
  "layer": "top",
  "position": "top",
  "height": 28,
  "margin-top": 0,
  "margin-left": 0,
  "margin-right": 0,

  "modules-left": [
    "hyprland/workspaces",
    "hyprland/window"
  ],

  "modules-right": [
    "network",
    "pulseaudio",
    "temperature",
    "cpu",
    "memory",
    "clock",
    "custom/power"
  ],

  "hyprland/workspaces": {
    "on-click": "hyprctl dispatch workspace e+1",
    "on-click-middle": "hyprctl dispatch workspace previous",
    "format": "{name}"
  },

  "hyprland/window": {
    "max-length": 60,
    "separate-outputs": true,
    "format": "{title}",
    "rewrite": {
      "^(.*)$": "  $1"
    }
  },

  "network": {
    "format": "{ifname}",
    "format-disconnected": " offline",
    "tooltip-format": "{ifname} {ipaddr}/{cidr}",
    "interval": 5
  },

  "pulseaudio": {
    "format": "{volume}%",
    "format-muted": "mute",
    "scroll-step": 2,
    "on-click": "pavucontrol"
  },

  "temperature": {
    "critical-threshold": 80,
    "format": "{temperatureC}°C",
    "interval": 5
  },

  "cpu": {
    "format": "{usage}%",
    "interval": 2
  },

  "memory": {
    "format": "{used}%"
  },

  "clock": {
    "format": "{:%d/%m %H:%M}",
    "tooltip-format": "{:%A, %d de %B de %Y}"
  },

  "custom/power": {
    "format": "",
    "tooltip": "Desligar/Logout",
    "on-click": "wlogout || hyprctl dispatch exit"
  }
}

